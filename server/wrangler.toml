name = "oler-helper"
main = "worker.js"
compatibility_date = "2024-01-01"

# 环境变量（部署时通过 wrangler secret 设置，不提交到 Git）
# wrangler secret put OPENAI_API_KEY
# wrangler secret put OPENAI_BASE_URL
# wrangler secret put OPENAI_MODEL

[vars]
# 只有非敏感变量放在这里
NODE_ENV = "production"
