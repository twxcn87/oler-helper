# 我用一个月时间，从零做了一个信息学竞赛AI助手，并把它做成小程序上线了

> 一个程序员父亲，如何为孩子的竞赛学习打造一款AI工具

---

## 一、起因：孩子学信息学，我做了点啥？

去年，我的孩子开始接触**信息学奥林匹克（OI）**——这是中国中小学生五大竞赛之一，需要用 C++ 写算法题解题。

作为程序员，我一开始觉得「这不就是刷题吗？」但深入了解后发现，这个过程远没有想象中简单：

- **题目平台多**：洛谷、Codeforces、牛客网……各平台格式不同
- **知识点抽象**：动态规划、树状数组、并查集……很多概念孩子理解困难
- **代码调试难**：CE（编译错误）、RE（运行错误）、WA（答案错误）、TLE（超时），孩子不知道问题在哪
- **资料分散**：算法讲解、例题、模板散落在各处，没有系统整理

我就想：**如果能有个 AI 助手，帮孩子快速理解题目、讲解算法、分析代码错误，该多好？**

于是，我花了一个月，从零做出了 **OIer-Helper**。

---

## 二、产品形态：三个版本，覆盖所有使用场景

OIer-Helper 最终做成了三个版本：

| 版本 | 适合场景 | 技术栈 |
|------|---------|--------|
| **网页版** | 孩子在家用电脑学习 | 纯前端，单文件部署 |
| **微信小程序** | 随时随地用手机刷题 | 微信原生开发 + Node.js 后端 |
| **桌面应用** | 本地离线使用 | Electron |

核心功能就五个：

1. **📝 题目解析** — 粘贴洛谷、Codeforces 等 OJ 链接，AI 分析题目、给出解题思路和代码模板
2. **📚 算法讲解** — 输入算法名称（如「动态规划」），系统讲解概念、步骤、代码模板
3. **🔍 代码 Debug** — 粘贴 C++ 代码，AI 帮你找出 CE/RE/WA/TLE 错误并给出修正方案
4. **✏️ 练习题推荐** — 告诉 AI 你学完了什么，推荐 3-5 道梯度难度练习题
5. **📒 笔记整理** — 输入知识点，生成结构化的学习笔记

---

## 三、踩过的坑：每一步都是教训

### 坑一：小程序不能访问 localhost

这是最早遇到的问题。我把小程序的 API 地址写成 `http://localhost:8080`，结果在模拟器里能跑，一换到真机就报错：

```
Cannot send network request to localhost
```

**原因**：微信小程序运行在微信的沙箱里，`localhost` 指向的是手机自己，不是我的电脑。

**解决**：把 API 地址改成局域网 IP `http://192.168.1.8:8080`，同时要在 `project.config.json` 里把 `urlCheck` 设为 `false`，并在开发者工具里勾选「不校验合法域名」。

---

### 坑二：路由器 AP 隔离让手机无法访问电脑

改完局域网 IP 后，真机还是访问不了。排查后发现是公司路由器的 **AP 隔离**功能——手机虽然连着同一个 WiFi，但被阻止访问同网段的其他设备。

**临时方案**：用 USB 数据线连接手机和电脑，通过 ADB reverse 端口转发：

```bash
adb reverse tcp:8080 tcp:8080
```

这样手机上的 `localhost:8080` 就转发到电脑的 `192.168.1.8:8080` 了。但缺点是需要一直插着数据线。

**永久方案**：部署公网服务器。

---

### 坑三：Vercel 在国内访问不了

一开始我想把后端 API 部署到 Vercel，代码推上去就自动发布，很方便。但部署后，小程序在国内完全打不开 Vercel 的域名——超时、连接失败。

GitHub 国内访问也慢，用 ghproxy.net 有时成功有时失败。

**解决**：放弃 Vercel，改用**腾讯云轻量应用服务器**（99 元/年那种），自己搭建 Node.js 后端，监听 `0.0.0.0` 暴露公网端口。

---

### 坑四：小程序需要 HTTPS

腾讯云服务器部署好了，IP 是 `49.235.106.148`，端口 3000。但微信小程序强制要求 **HTTPS**，我的服务器只跑了 HTTP。

买域名配证书太麻烦，也不想花钱。

**解决**：用了 **Cloudflare Tunnel**（免费）。在服务器上运行一条命令：

```bash
nohup cloudflared tunnel --url http://localhost:3000 > /tmp/tunnel.log 2>&1 &
```

Cloudflare 会给你一个 `https://xxx.trycloudflare.com` 的 HTTPS 地址，直接代理到本地服务器。完美解决。

⚠️ 但这个地址每次重启服务器会变，所以写了一个 `monitor-tunnel.ps1` 监控脚本，配合服务器 SSH 密钥，可以实现自动检测+重启。

---

### 坑五：测试号不能发布

小程序开发初期，我用的是微信给的**测试号**（AppID: `wx8ddc48fbecad6aab`）。代码都写好了，想发布给更多人用，才发现测试号不能上架。

**解决**：去 [mp.weixin.qq.com](https://mp.weixin.qq.com) 重新注册了一个正式小程序账号（个人类型，免费），拿到新 AppID `wxa542c18af0630832`，替换掉项目配置，重新上传代码。

---

### 坑六：审核期间的各种填报细节

提交审核时才知道，小程序名称、简介、截图都有严格要求：

- **名称**：必须是全称，不能包含「官方」「旗舰」等词汇
- **简称**：必须是名称的连续子串，4-10 字符
- **简介**：不能超过 120 字节（中文每个字算 3 个字节！之前按字符数算，提交了两次都被打回）
- **功能截图**：至少一张，要展示核心功能界面

最终名称定为 **「OI算法AI助手」**，简称 **「算法AI助手」**，简介控制在 50 字以内。

---

## 四、技术架构

```
┌─────────────────────────────────────────────────┐
│                  客户端                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
│  │  网页版   │  │  微信小程序 │  │  Electron桌面版 │  │
│  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
└───────┼─────────────┼───────────────┼──────────┘
        │             │               │
        └─────────────┴───────┬───────┘
                              ▼
                    ┌─────────────────┐
                    │  Cloudflare     │
                    │    Tunnel       │
                    │ https://xxx...  │
                    └────────┬────────┘
                             ▼
                    ┌─────────────────┐
                    │  腾讯云服务器     │
                    │  49.235.106.148 │
                    │  :3000          │
                    │                 │
                    │  Node.js        │
                    │  server.js      │
                    │  + AI API       │
                    └─────────────────┘
```

**后端**：Node.js + Express，调用 Agnes AI API（兼容 OpenAI 格式）做题目解析和算法讲解。

**关键依赖**：
- 后端：`express`、内置 `fetch()`（Node 18+）
- 小程序：微信原生开发，`rich-text` 组件渲染 Markdown
- 桌面端：Electron v30，加载远程 Web 页面

---

## 五、现在怎么样了？

截至 2026 年 8 月 30 日：

- ✅ **三个版本全部正常运行**
- ✅ **服务器在线**，Tunnel 稳定
- ✅ **小程序已提交审核**，等待通过
- ✅ **源代码开源**在 GitHub：https://github.com/twxcn87/oler-helper

审核通过后，别人就能在微信搜索「OI算法AI助手」找到它。

---

## 六、写在最后

做一个产品，最难的从来不是写代码——代码只是手段。

真正难的是：

- 理解用户（孩子）真正需要什么
- 在技术限制面前找可行方案（AP 隔离 → ADB；HTTP → Cloudflare Tunnel）
- 一次一次提交、被打回、再修改（简介字数踩了两次坑）

这个小工具不一定完美，但它是我为孩子学习做的第一件事。

希望未来它能帮到更多OIer。

---

*如果你对这个项目感兴趣，欢迎 star 我的 GitHub：https://github.com/twxcn87/oler-helper*
